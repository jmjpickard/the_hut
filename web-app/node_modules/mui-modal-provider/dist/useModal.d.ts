import { ShowFn } from './types';
declare type Options = {
    disableAutoDestroy?: boolean;
};
export default function useModal(options?: Options): {
    state: import("./types").State;
    updateModal: import("./types").UpdateFn;
    hideModal: import("./types").HideFn;
    destroyModal: import("./types").DestroyFn;
    showModal: ShowFn;
};
export {};
