import { State, StateElement, ActionMap, ModalComponentProps, Props } from './types';
export declare enum Types {
    SHOW = "SHOW",
    HIDE = "HIDE",
    UPDATE = "UPDATE",
    DESTROY = "DESTROY",
    DESTROY_BY_ROOT_ID = "DESTROY_BY_ROOT_ID",
    UNKNOWN = "UNKNOWN"
}
declare type Payload = {
    [Types.SHOW]: StateElement & {
        id: string;
    };
    [Types.HIDE]: {
        id: string;
    };
    [Types.UPDATE]: {
        id: string;
        props: ModalComponentProps<Props>;
    };
    [Types.DESTROY]: {
        id: string;
    };
    [Types.DESTROY_BY_ROOT_ID]: {
        rootId: string;
    };
    [Types.UNKNOWN]: undefined;
};
declare type Action = ActionMap<Payload>[keyof ActionMap<Payload>];
export declare const initialState: State;
export default function reducer(state: State, action: Action): State;
export {};
